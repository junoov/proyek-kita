<section class="intro" aria-labelledby="intro-title">
  <div class="intro__lead">
    <p class="intro__eyebrow">01 / Cara kami bekerja</p>
    <h2 id="intro-title">Keputusan penting dibuat sebelum kain dipotong.</h2>
    <p class="intro__description">
      Kami menerjemahkan kebutuhan tim menjadi brief yang bisa dikerjakan: fungsi pakaian, pilihan bahan, ukuran, dan detail identitas yang tidak hilang di lantai produksi.
    </p>
  </div>

  <div class="intro__brief">
    <div class="intro__brief-head">
      <span>PRODUCTION BRIEF</span>
      <span>01—03</span>
    </div>
    <ol>
      <li>
        <span>01</span>
        <div>
          <strong>Fungsi lebih dulu</strong>
          <p>Jenis pekerjaan dan ritme pemakaian menentukan bentuk awal seragam.</p>
        </div>
      </li>
      <li>
        <span>02</span>
        <div>
          <strong>Material yang masuk akal</strong>
          <p>Kain dan warna dipilih dari kebutuhan nyata, bukan hanya dari layar.</p>
        </div>
      </li>
      <li>
        <span>03</span>
        <div>
          <strong>Sampel sebelum batch</strong>
          <p>Detail, ukuran, dan penempatan identitas diperiksa sebelum produksi berjalan.</p>
        </div>
      </li>
    </ol>
  </div>
</section>

<style>
  .intro {
    background: var(--color-signal);
    color: var(--color-text-inverse);
    display: grid;
    gap: var(--space-10);
    padding: clamp(3.5rem, 9vw, 7rem) var(--layout-edge);
  }

  .intro__lead,
  .intro__brief {
    margin: 0 auto;
    max-width: var(--layout-max);
    width: 100%;
  }

  .intro__lead {
    display: grid;
    gap: var(--space-5);
  }

  .intro__eyebrow,
  .intro__brief-head {
    color: var(--color-mist);
    font-size: var(--type-label);
    font-weight: var(--font-weight-bold);
    letter-spacing: 0.1em;
    margin: 0;
    text-transform: uppercase;
  }

  .intro h2 {
    color: var(--color-text-inverse);
    font-family: var(--font-display);
    font-size: clamp(2.55rem, 7vw, 5.7rem);
    font-weight: var(--font-weight-bold);
    letter-spacing: -0.08em;
    line-height: 0.91;
    margin: 0;
    max-width: 10ch;
  }

  .intro__description {
    color: color-mix(in oklch, var(--color-text-inverse) 72%, transparent);
    font-size: var(--type-lead);
    line-height: 1.55;
    margin: 0;
    max-width: 38rem;
  }

  .intro__brief {
    border-top: 1px solid color-mix(in oklch, var(--color-text-inverse) 35%, transparent);
  }

  .intro__brief-head {
    align-items: center;
    color: var(--color-mist);
    display: flex;
    justify-content: space-between;
    padding: var(--space-3) 0;
  }

  .intro__brief-head span:last-child {
    color: color-mix(in oklch, var(--color-text-inverse) 55%, transparent);
  }

  .intro ol {
    border-top: 1px solid color-mix(in oklch, var(--color-text-inverse) 35%, transparent);
    display: grid;
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .intro li {
    border-bottom: 1px solid color-mix(in oklch, var(--color-text-inverse) 22%, transparent);
    display: grid;
    gap: var(--space-4);
    grid-template-columns: 2rem minmax(0, 1fr);
    padding: var(--space-4) 0;
  }

  .intro li > span {
    color: var(--color-mist);
    font-size: var(--type-small);
    font-weight: var(--font-weight-bold);
  }

  .intro li div {
    display: grid;
    gap: var(--space-1);
  }

  .intro li strong {
    color: var(--color-text-inverse);
    font-size: var(--type-body);
    font-weight: var(--font-weight-bold);
  }

  .intro li p {
    color: color-mix(in oklch, var(--color-text-inverse) 66%, transparent);
    font-size: var(--type-small);
    line-height: 1.5;
    margin: 0;
    max-width: 42rem;
  }

  @media (min-width: 48rem) {
    .intro {
      column-gap: var(--space-10);
      grid-template-columns: minmax(0, 0.9fr) minmax(0, 1.1fr);
      padding-inline: max(var(--layout-edge), calc((100vw - var(--layout-max)) / 2));
    }

    .intro__lead,
    .intro__brief {
      margin: 0;
    }

    .intro__lead {
      align-content: start;
      grid-column: 1;
    }

    .intro__brief {
      align-self: end;
      grid-column: 2;
    }
  }

  @media (min-width: 64rem) {
    .intro {
      column-gap: var(--space-16);
      grid-template-columns: minmax(18rem, 0.8fr) minmax(34rem, 1.2fr);
    }
  }
</style>
